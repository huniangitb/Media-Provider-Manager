// PhotoView/build.gradle

// 定义扩展属性，供下级子模块 photoview 使用
ext {
    // 这里统一定义版本，防止 photoview 找不到变量
    sdkVersion = 34
    minSdkVersion = 29
}

// 注意：删除原有的 buildscript 块，因为插件版本由根目录统一管理
// 注意：删除原有的 allprojects 块，因为仓库地址已在根目录配置